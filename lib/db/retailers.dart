
class UserServices{

  String ref = "retailers";

  createUser(Map value){
    // _database.reference().child(ref).push().set(
    //     value
    // ).catchError((e) => { print(e.toString())});
  }
}